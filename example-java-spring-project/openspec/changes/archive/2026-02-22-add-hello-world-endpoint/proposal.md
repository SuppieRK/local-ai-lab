## Why

We need to add a new endpoint that returns "Hello, World!" message. This will demonstrate basic API functionality in our application and serve as a simple example for developers to understand how to create endpoints.

## What Changes

- Add a new REST endpoint at /api/hello-world that returns a JSON response with "message": "Hello, World!"
- Create the necessary controller method to handle this endpoint
- Add appropriate HTTP response headers and status codes for proper API behavior

## Capabilities

### New Capabilities
- hello-world-endpoint: Endpoint that returns "Hello, World!" message

### Modified Capabilities

## Impact

This change will add a basic endpoint to the application that can be used for testing purposes. It will not affect any existing functionality but will provide a simple demonstration endpoint that developers can use as a reference when implementing other endpoints.
