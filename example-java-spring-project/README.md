# Example Java Spring Project

This project demonstrates how to build a Spring Boot application.

## Available Endpoints

### Hello World Endpoint

GET `/api/hello-world`

Returns a JSON response with "Hello, World!" message.

Example Response:
```json
{
  "message": "Hello, World!"
}
```